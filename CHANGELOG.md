# Changelog

All notable changes to this configuration will be documented in this file.

## [1.0.0] - 2026-01-03

### Added
- Initial release
- Core commands: `/next-task`, `/pr-fix`, `/sync-branch`, `/check-ci`, `/project-status`, `/suggest`
- Utility commands: `/commit`, `/push`, `/setup-claude`, `/update-claude-config`
- Shared permissions for git, gh, pnpm, and common utilities
- README with setup instructions
